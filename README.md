# ImageRecommendation_ProductMatching

Developing a script to perform image recognition on input images and return the most similar product from product catalog by generating vector embeddings and searching through vector databases.
